export class Product {
    sku: string;
    name: string;
    description: string;
    category: string;
    pics: Array<string>;
    sizes: Array<string>;
    tags: Array<string>;
    price: number;
}